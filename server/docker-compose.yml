version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: medquad_mongodb
    ports:
      - 27017:27017
    volumes:
      - medquad_db_data:/data/db
    restart: unless-stopped

volumes:
  medquad_db_data:
